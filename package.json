{
  "name": "store",
  "version": "0.0.1",
  "description": "Reactive store using rxjs",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brady Sterling",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.0.3"
  }
}
